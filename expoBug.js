This error typically occurs when there's a mismatch between the native modules used in your Expo app and the version of Expo CLI you're using.  It often manifests during the build process, especially when using EAS Build or building standalone apps.